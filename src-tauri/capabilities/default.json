{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the AutoMD main window — notably core:event so the frontend can listen() for native-menu actions.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
